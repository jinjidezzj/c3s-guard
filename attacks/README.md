# Attack Entry Points

This directory contains representative attack entry points used by the artifact:

- `badnet.py`
- `sig.py`
- `wanet.py`

These files are copied for inspection and provenance. The experiment runner still resolves the full repository attack stack from the repository root.
